$('#member-count-<%= @game.id.to_s %>').html('<%= @game.users.count if @game.users.present? %>');
$('#join-leave-<%= @game.id.to_s %>').html('<%= escape_javascript(link_to "You in?  Let me know.", '/games/' + @game.id + '/join', {:class => 'button large-12 columns', :remote => true}) %>');