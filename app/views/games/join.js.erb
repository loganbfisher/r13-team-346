$('#member-count-<%= @game.id.to_s %>').html('<%= @game.users.count %>');
$('#join-leave-<%= @game.id.to_s %>').html('<%= escape_javascript(link_to "Leave Game", "/games/" + @game.id + "/leave", {:remote => true}) %>');