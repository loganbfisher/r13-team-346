$('#member-count-<%= @game.id.to_s %>').html('<%= @game.users.count %>');
$('#join-leave-<%= @game.id.to_s %>').html('<%= escape_javascript(link_to "I'm not going anymore..", "/games/" + @game.id + "/leave", {:class => "button large-12 columns", :remote => true}) %>');